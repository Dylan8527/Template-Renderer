# Template-Renderer

​	Here is a simple implementation for Renderer with OpenGL + Dear ImGui.

## Compile

```bash
mkdir build
cd build
cmake ..
cmake --build . --config Release -j
cd Release
./main.exe
```

